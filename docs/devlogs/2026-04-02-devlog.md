# Devlog - 2026-04-02

## 今日主题
今天的工作重点，是把原本分散的三份静态前端稿真正接到现有后端上，并整理成一个可运行、可演示、可继续微调的单页版本。

这次没有继续沿用旧的 Vue 根页面作为最终展示壳，而是把静态稿能力直接收拢成单页工作台：

1. `workbench`
2. `report`
3. `assistant`

三块内容都留在同一个页面内，通过前端状态切换视图，不再依赖 URL 页面跳转。

## 完成内容

- 将 `first / second / third` 三份静态稿的核心能力合并为单页静态壳：
  - 工作台上传与评审启动
  - 报告展示
  - 助手追问
- 重写了静态壳主文件：
  - [first page code_integrated.html](/Users/ann/.cursor/worktrees/awesome-requirement-review-agent-main/uop/frontend/first%20page%20code_integrated.html)
- 发布了 Vite 可直接访问的静态入口：
  - [single-page-shell.html](/Users/ann/.cursor/worktrees/awesome-requirement-review-agent-main/uop/frontend/public/single-page-shell.html)
- 将前端根入口切到静态壳：
  - [index.html](/Users/ann/.cursor/worktrees/awesome-requirement-review-agent-main/uop/frontend/index.html)
- 前端静态壳已经接入以下后端接口：
  - `POST /api/review/upload`
  - `POST /api/review/config`
  - `POST /api/review/start`
  - `GET /api/review/stream/{session_id}`
  - `GET /api/review/report/{session_id}`
  - `GET /api/review/status/{session_id}`
  - `POST /api/review/chat`
  - `POST /api/review/reset`
  - `POST /api/review/share`
  - `GET /api/review/export/pdf/{session_id}`
- API 基地址统一改成：
  - `http://127.0.0.1:8005/api`
- 报告页的问题卡片现在可以直接把上下文送进助手视图，不再走新页面。
- 增加了 `sessionStorage` 状态恢复，刷新后仍能保留当前 session、报告和对话上下文。

## 入口与运行链路修正

今天中间有一个关键问题被确认并修复：

- 虽然静态壳已经写好，但 Vite 根入口一开始仍然在加载旧的 Vue `App.vue`。
- 这导致浏览器访问 `http://127.0.0.1:5176/` 时，看到的还是旧版界面，而不是新单页壳。

处理结果：

- 根入口 [index.html](/Users/ann/.cursor/worktrees/awesome-requirement-review-agent-main/uop/frontend/index.html) 已改为直接跳转到 `/single-page-shell.html`
- 访问 `http://127.0.0.1:5176/` 现在会进入新的单页静态工作台

## UI 调整记录

在单页壳打通后，又做了几轮快速 UI 微调，主要是为了把页面从“能跑”整理到“能看”：

- 主色改成纯橙色系，最终保留为：
  - `#ef6c00`
- 成功态保持绿色，不和主色混用。
- 左侧品牌标题和页面主标题改成黑色，提高阅读稳定性。
- 删除了 Workbench 顶部那组冗余摘要卡片：
  - `未上传`
  - `等待`
  - `未生成`
  - `0 项`
- 调整了助手页头部操作按钮布局：
  - `分享`
  - `导出 PDF`
  - `重新评审`
  现在固定为同一行，窄屏下允许横向滚动，不再挤成两行。

## 新增测试

今天补了几类针对静态壳的前端测试，避免入口、布局和样式回退：

- [staticSinglePageShell.test.mjs](/Users/ann/.cursor/worktrees/awesome-requirement-review-agent-main/uop/frontend/tests/staticSinglePageShell.test.mjs)
- [rootEntryUsesStaticShell.test.mjs](/Users/ann/.cursor/worktrees/awesome-requirement-review-agent-main/uop/frontend/tests/rootEntryUsesStaticShell.test.mjs)
- [staticShellColorTokens.test.mjs](/Users/ann/.cursor/worktrees/awesome-requirement-review-agent-main/uop/frontend/tests/staticShellColorTokens.test.mjs)
- [workbenchRemovesSummaryCards.test.mjs](/Users/ann/.cursor/worktrees/awesome-requirement-review-agent-main/uop/frontend/tests/workbenchRemovesSummaryCards.test.mjs)
- [assistantHeaderActionsRow.test.mjs](/Users/ann/.cursor/worktrees/awesome-requirement-review-agent-main/uop/frontend/tests/assistantHeaderActionsRow.test.mjs)

## 验证结果

今天反复执行并确认通过的命令包括：

- `node --test tests/staticSinglePageShell.test.mjs`
- `node --test tests/rootEntryUsesStaticShell.test.mjs`
- `node --test tests/staticShellColorTokens.test.mjs`
- `node --test tests/workbenchRemovesSummaryCards.test.mjs`
- `node --test tests/assistantHeaderActionsRow.test.mjs`
- `npm test`
- `npm run build`

同时确认了本地运行状态：

- 后端运行在 `http://127.0.0.1:8005`
- 前端运行在 `http://127.0.0.1:5176`

## 当前状态

- 单页静态壳已经成为当前可访问的前端入口。
- 三个视图的核心链路已经接通后端。
- 页面样式已经完成第一轮可用性修正。
- 当前更适合继续做细节打磨，而不是再回到多页面拆分模式。

## 下一步建议

- 继续按用户实机反馈微调布局，而不是再大范围重做结构。
- 优先处理报告页和助手页的密度、按钮层级、空状态表现。
- 如果后续确认静态壳就是主线，可以逐步下掉旧 Vue 展示壳，避免双入口继续混淆。
